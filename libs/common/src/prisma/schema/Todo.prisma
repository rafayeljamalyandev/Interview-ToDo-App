model Todo {
  id        Int     @id @default(autoincrement())
  title     String
  completed Boolean @default(false)
  userId    Int

  user      User    @relation("User Todos", fields: [userId], references: [id], onDelete: Cascade) 
}
